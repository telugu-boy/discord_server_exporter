{
  "description": "Represents a single role",
  "properties": {
    "color": {
      "description": "The colour of the role, represented as a 24 bit int",
      "exclusiveMaximum": 16777216,
      "minimum": 0,
      "type": "integer"
    },
    "hoisted": {
      "description": "Whether or not the role is hoisted",
      "type": "boolean"
    },
    "id": {
      "description": "The ID of the role. Used for identification purpose internally",
      "type": "string"
    },
    "mentionable": {
      "description": "Whether or not the role is mentionable",
      "type": "boolean"
    },
    "name": {
      "description": "The name of the role",
      "type": "string"
    },
    "permission_value": {
      "description": "The permission integer",
      "type": "string"
    },
    "position": {
      "description": "The position of the role in the heirarchy",
      "maximum": 250,
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [ "name", "color", "mentionable", "permission_value", "position" ],
  "title": "Discord role",
  "type": "object"
}
