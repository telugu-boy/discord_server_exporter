{
  "title": "Discord channel",
  "description": "Represents a single channel",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the channel",
      "type": "string"
    },
    "role_permission_overrides": {
      "description": "An array of role permission overrides",
      "type": "array",
      "items": { "$ref": "permission_setting_schemas.json#/role_permission_settings_schema" }
    },
    "user_permission_overrides": {
      "description": "An array of role permission overrides",
      "type": "array",
      "items": { "$ref": "permission_setting_schemas.json#/user_permission_settings_schema" }
    }
  }
}