{
  "title": "Discord voice channel",
  "description": "Represents a single voice channel",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the channel",
      "type": "string"
    },
    "bitrate": {
      "description": "The voice channel bitrate",
      "maximum": 256,
      "minimum": 8,
      "type": "integer"
    },
    "user_limit": {
      "description": "The user limit for this channel",
      "maximum": 99,
      "minimum": 0,
      "type": "integer"
    },
    "role_permission_overrides": {
      "description": "An array of role permission overrides",
      "type": "array",
      "items": { "$ref": "permission_setting_schemas.json#/role_permission_settings_schema/properties" }
    },
    "user_permission_overrides": {
      "description": "An array of role permission overrides",
      "type": "array",
      "items": { "$ref": "permission_setting_schemas.json#/user_permission_settings_schema/properties" }
    }
  },
  "required": [ "name", "bitrate", "user_limit" ]
}